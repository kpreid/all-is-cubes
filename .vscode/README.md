This directory contains configuration files for Visual Studio Code.

In order to obtain a `tasks.json` file with tasks for running All is Cubes tests and related things,
run `cargo xtask init`.
This file is not in version control because it is partly generated from the workspace metadata.
