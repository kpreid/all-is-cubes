[package]
name = "test-renderers"
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Rendering tests for all-is-cubes."
publish = false

# Tests use a custom test harness; among other reasons, we want to explicitly skip the
# tests if there is no GPU on the system rather than just making them “pass” falsely.

[lib]
doctest = false
# Disable running as benchmark so that the default doesn't interfere with Criterion usage.
bench = false

[[bin]]
name = "bless-render"
test = false
doctest = false
bench = false

[[test]]
# Note: This test has its dependencies disabled and thus doesn’t compile.
# It has its dependencies disabled because it was written for Bevy 0.16.0 and we don’t want both
# newer and older Bevy crates to end up in the lock file.
# TODO: Figure out what’s wrong and improve it.
#
# Dependencies we would have if this were runnable:
# bevy = { version = "0.16.0", optional = true, default-features = false, features = ["bevy_animation", "bevy_core_pipeline", "bevy_gltf", "bevy_pbr", "bevy_render", "bevy_scene", "bevy_window", "dynamic_linking", "multi_threaded", "png"]}
# bevy_gltf = { version = "0.16.0", features = ["bevy_animation"] }
name = "gltf-render"
doctest = false
harness = false
test = false # Exclude from normal test runs because it does not pass yet!
required-features = ["gltf"]

[[test]]
name = "ray-render"
doctest = false
harness = false

[[test]]
name = "wgpu-render"
doctest = false
harness = false

[[test]]
name = "ui"
doctest = false
harness = false

[features]
default = ["auto-threads"]
# This is a feature so that, if we want to, we can test the non-threaded build.
auto-threads = [
    "all-is-cubes/auto-threads",
    "all-is-cubes-gpu/auto-threads",
    "all-is-cubes-port/auto-threads",
    "all-is-cubes-render/auto-threads",
]
# Enables `gltf-render` test target. Doesn’t work — see comments above on the [[test]].
gltf = [
    "dep:all-is-cubes-port",
    "dep:bytemuck",
    # "dep:bevy",
    # "dep:half",
    # "dep:manyfmt",
    "tokio/fs",
]

[dependencies]
all-is-cubes = { workspace = true, features = ["save"] }
all-is-cubes-content = { workspace = true }
all-is-cubes-gpu = { workspace = true, features = ["wgpu"] }
# used only for gltf export+render tests
all-is-cubes-port = { workspace = true, optional = true, features = ["export", "gltf"] }
all-is-cubes-render = { workspace = true, features = ["raytracer", "serde", "std"] }
all-is-cubes-ui = { workspace = true, features = ["session"] }
anyhow = { workspace = true }
async_fn_traits = { workspace = true }
bytemuck = { workspace = true, optional = true }
# Using clap for parsing test command-line arguments
clap = { workspace = true }
exhaust = { workspace = true }
futures-core = { workspace = true }
#futures-channel = { workspace = true, optional = true }
futures-util = { workspace = true, features = ["std"] }
image = { workspace = true, features = ["png"] }
imgref = { workspace = true }
itertools = { workspace = true }
log = { workspace = true }
# Used by the `bless-render` tool to compress images that are to be stored in the repository.
oxipng = { version = "10.0.0", default-features = false, features = ["parallel", "zopfli"] }
rendiff = { workspace = true }
send_wrapper = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
simplelog = { workspace = true }
tempfile = { workspace = true }
time = { workspace = true }
tinytemplate = { workspace = true }
# Using tokio for async test-running.
tokio = { workspace = true, features = ["macros", "rt", "rt-multi-thread", "parking_lot", "sync", "time"] }
# In addition to depending on wgpu directly, we also need to enable backends for the tests to
# actually find an adapter. Enabling default features suffices (as of wgpu 24.0).
wgpu = { workspace = true, default-features = true }

[lints]
workspace = true
