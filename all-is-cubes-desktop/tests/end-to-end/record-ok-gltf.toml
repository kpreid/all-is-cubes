bin.name = "all-is-cubes"
# Recording just the empty for now because that will be less exciting and bulky than
# actual meshes. Maybe revisit this later.
args = "--no-config-files --template blank --graphics record -o test-record-blank.gltf"
